name: Mars CD for PyPI

on: [push]

jobs:
  build:
    name: Build wheels on ${{ matrix.os }} for ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macOS-latest, windows-latest]
        node-version: ["14.x"]
        arch: [auto]
        include:
          - os: ubuntu-latest
            arch: aarch64
          - os: macOS-latest
            arch: universal2            

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: 3.10

      - name: Set up QEMU
        if: ${{ matrix.arch == 'aarch64' }}
        uses: docker/setup-qemu-action@v1

      - name: Build wheels
        uses: joerick/cibuildwheel@v2.3.1
        env:
          CIBW_BEFORE_BUILD: >
              pip install -r ci/requirements-wheel.txt
          CIBW_ARCHS: ${{ matrix.arch }}
          CIBW_SKIP: pp* *-musllinux* cp36-* *-win32 *_i686 
          
      - uses: actions/upload-artifact@v2
        with:
          path: ./wheelhouse/*.whl
          
